/**
 * VMailx — NotificationProvider Interface (Phase 2 Scaffold)
 *
 * Extension point for in-app notifications, email alerts, webhooks.
 * DO NOT implement in Phase 1.
 */

// eslint-disable-next-line @typescript-eslint/no-empty-interface
export interface NotificationProvider {
  // Phase 2 — not implemented
  //
  // Planned methods:
  //   notifyNewEmail(userId: string, emailSummary: EmailSummary): Promise<void>
  //   notifyDomainVerified(userId: string, domain: string): Promise<void>
  //   notifyQuotaWarning(userId: string, mailboxId: string, pct: number): Promise<void>
}
